export const showSuccessMessage = (success) => (
  <div className="alert alert-success">{success}</div>
);

export const showErrorMessage = (success) => (
  <div className="alert alert-danger">{success}</div>
);
